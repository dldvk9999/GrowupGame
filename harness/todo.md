# 알려진 미구현/TODO 후보

- ~~로비 채팅 UI 미연결~~ → 해결됨(`LobbyChat.jsx` 연결, [`social-chat.md`](./social-chat.md))
- 외부 이미지(실사/일러스트) 미적용 — `MonsterSprite`는 `VITE_SPRITE_CDN_URL` 세팅 시 이미지를 자동 우선 사용하도록 이미 확장 가능한 구조
- 사육장(보유 몬스터 목록) 화면 없음 — 현재는 활성 몬스터 1마리만 운용(설계상 의도). ~~도감~~ → **부분 해결**: 마이페이지에 `MonsterDex.jsx`(9종 진화 트리 발견 현황) 추가. 다만 "발견"이 서버 기록이 아니라 현재 활성 몬스터 기준 매 계산이라, 나중에 포획/전환 기능이 생기면 실제 기록 테이블로 바꿔야 함
- ~~PvP~~ → 해결됨(비동기 매칭 전투, [`pvp.md`](./pvp.md)). 몬스터 포획(교체) 기능은 여전히 없음(보스 처치=자동 성장 개념으로 대체, "포획" 요건은 스타터 선택으로 단순화됨)
- ~~장비/코스튬이 스프라이트에 미반영~~ → **코스튬은 해결됨**(042, 슬롯별 등급색 배지 오버레이 + 인벤토리 코스튬 서브탭 착용/해제). 일반 장비는 여전히 미반영
- ~~PvP 전투력 계산에 장비/스킬 보너스 미포함~~ → **장비는 해결됨**(051, `calc_equipped_stat_bonus`로 PvP/랭킹/`fetch_my_combat_power`에 반영). **스킬 보유효과는 여전히 미반영** — 50종 계산식을 SQL로 통째로 포팅해야 해서 범위가 큼. 스킬 비중이 커지면 재검토
- ~~2·3단계 진화 스프라이트 없음~~ → 해결됨(9종 벡터 스프라이트 완성)
- ~~마이페이지/닉네임 수정~~ → 해결됨(닉네임 1회 수정)
- ~~스킬 커스터마이징~~ → 해결됨(뽑기/합성/편성 시스템)
- ~~로그아웃 시 상태 초기화가 일부만 됨~~ → **해결됨** — `handleSession(null)`이 게임 데이터 state 전부를 초기값으로 리셋. 순수 UI 트랜지언트 플래그는 다음 액션에서 자연히 덮어써지므로 제외
- **`profiles.stamina`/`stamina_max`/`stamina_updated_at` 컬럼이 001부터 완전히 미사용**(재검토 중 발견) — "스태미나 소모형 입장 제한" 초기 설계의 잔재로 추정, 실제론 던전이 "하루 3회" 카운터(`dungeon_attempts`)로 구현됨. **일부러 활용하지 않음**: 자유로운 자동사냥/전투가 이미 게임성의 일부라, 소모 시스템을 새로 붙이면 기존 경험을 제약하는 큰 변경이 되므로 사용자 승인 없이 진행 안 함. 정리(컬럼 제거)도 급하지 않아 우선순위 낮음
- **`monster_species.is_boss` 컬럼도 마찬가지로 미사용**(시드 데이터에 전부 `false`, 어디서도 조회 안 됨) — 스테이지 보스는 `stages.js`가 계산으로 만드는 가상 몬스터라 "보스 전용 종족" 개념 자체가 다른 방식으로 대체됨. 우선순위 낮음
- **용의 버프(20배)가 PvP/랭킹 서버 계산에 미반영**([`world-boss.md`](./world-boss.md) 참고) — 스테이지/던전에만 적용, PvP/랭킹은 그대로. 버그인지 의도된 공정성 설계인지 불확실해 사용자 확인 없이 손대지 않음
- **속성 상성(불/물/풀) 시스템이 아예 없음**(신규 발견, 105차 라운드 검토 중) — `combat.js`에 element 관련 로직이 전혀 없어, 스타터를 fire/water/grass 중 무엇을 골라도 전투에 실질적 차이가 없음(그냥 스킨 차이). 3속성 몬스터 수집형 게임 장르에서 흔히 기대되는 "상성"(예: 물>불>풀>물 1.2~1.5배 데미지) 요건인 건 맞지만, 도입하면: (1) 스테이지/던전 난이도 곡선 전체 재검증 필요(현재 데미지 공식이 상성 미적용 기준으로 튜닝됨), (2) PvP가 "스타터 선택(영구 고정)에 따른 상대 매칭 유불리"라는 완전히 새로운 공정성 문제가 생김(전직처럼 나중에 바꿀 수 없는 구조라 특히 민감), (3) 월드보스/던전 몬스터의 속성 배정도 다시 설계해야 함 — **파급 범위가 매우 커서 사용자 승인 없이 진행하지 않음.** 원하면 "상성 있이" vs "상성 없이 소소한 속성 전용 보너스만"(예: 도감/코스튬처럼 순수 수집 요소로만 유지) 중 방향을 먼저 정하고 시작하는 게 안전함.
- **PvP 복수전(migration 108)이 전투력 격차 제한 없이 아무나 지목 가능**(신규 발견) — 강한 유저가 약한 유저를 반복 지목해 사실상 확정승을 파밍할 여지 있음. 107의 "패배해도 위로보상"이 완충 역할을 하고 2초 쿨다운도 있어 당장 심각하진 않다고 판단해 일단 그대로 둠 — 실제 플레이 반응을 보고 어뷰징 신호가 뚜렷하면 전투력 격차 제한(예: ±50%) 추가 검토. 자세한 내용은 [`pvp.md`](./pvp.md)
- **연승 던전(155) `streak_dungeon_sessions.streak`가 이론상 int4 오버플로 가능**(신규 콘텐츠 배포 전 검토 중 발견, 저위험) — `continue_streak_dungeon`의 안티치트 게이트가 초당 1회로 제한하므로 오버플로 지점(약 21억)까지 도달하려면 68년 이상 연속 호출이 필요해 현실적 위험은 없다고 판단, 원인은 문서화만 해둠([`streak-dungeon.md`](./streak-dungeon.md))
- **연승 던전 승리 직후 "수령" 클릭 시 레벨 반영 타이밍 미세 오차 가능**(신규 콘텐츠) — `persistMonsterGrowth`가 fire-and-forget 비동기 호출이라, 이론상 서버가 참조하는 레벨이 한 틱 뒤처질 수 있음. 1초 안티치트 게이트가 사실상 충분히 커버해 실사용 체감 어려움, 발생 빈도 보이면 재검토([`streak-dungeon.md`](./streak-dungeon.md))
- **길드 레이드(158) 진입 중 길드를 탈퇴/변경해도 화면 캐시가 즉시 갱신 안 됨**(신규 콘텐츠, 저위험) — `guildRaid` 클라이언트 state가 던전 탭 재진입/새로고침 전까지 이전 길드 정보를 잠깐 보여줄 수 있음. 서버 함수(`enter_guild_raid` 등)는 항상 `my_guild_id()`로 최신 소속을 재확인하므로 실제 데미지/보상 반영은 항상 정확함 — 화면 표시만의 문제라 우선순위 낮음([`guild-raid.md`](./guild-raid.md))
- **정예레벨(168, 사용자 요청) 기반 던전 입장 제한 — 예정된 후속 작업(아직 미구현)**: 레벨 500 만렙 이후 별도로 오르는 "정예레벨"(최대 100) 시스템을 신설했고, 사용자가 이 레벨을 기준으로 **일부 던전의 입장 조건을 제한**할 계획이라고 명시함. 아직 어떤 던전을, 어떤 정예레벨부터 막을지 방향이 정해지지 않아서 구현은 보류 — 다음에 착수할 때 확인할 것들:
  - 대상 던전(예: 길드 레이드? 봉인된 던전? 새로운 엔드게임 던전?)
  - 게이트 방식(특정 정예레벨 미만이면 아예 입장 불가 vs 보상만 차등)
  - 기존 유저들(정예레벨 0인 상태로 이미 만렙인 유저) 대상 유예 기간이 필요한지
- **길드 로비(`GuildLobby.jsx`, 사용자 요청) — 지금은 장식용 SVG 아트워크 + 안내문구만 있고 실제 컨텐츠는 없음.** 후속으로 추가하면 좋을 것들(우선순위 순 아님, 방향 논의 필요):
  - 길드 전용 채팅(현재 로비 채팅은 서버 전체 공용, [`social-chat.md`](./social-chat.md) 참고 — 길드 채널 분리는 별도 테이블/RLS 설계 필요)
  - 길드 창고(길드원끼리 아이템/골드 기부·인출) — 어뷰징 방지 설계가 까다로움(기부만 되고 인출은 길드장 승인제로 하는 등)
  - 길드 레벨/경험치(길드원들의 활동으로 성장, 레벨업 시 소소한 길드 전체 버프) — [`guild.md`](./guild.md)의 "알려진 한계"에도 이미 언급된 항목과 연결
  - 길드 스킬/버프(길드 레벨이나 레이드 클리어 횟수로 해금되는 길드원 전체 공통 버프)
  - 로비 화면 자체에 길드원 아바타/몬스터 스프라이트를 배치해서 "누가 지금 접속해있나" 보여주는 연출(실시간 접속 상태 추적 인프라 필요)
